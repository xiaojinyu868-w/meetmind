# v2.1 — 跨浏览器课堂证据同步（主链路已完成）

> 创建于 2026-04-25，2026-07-16 完成主链路。本文保留历史根因与最终决策，供后续演进参考。

## 2026-07-16 当前进展

- ✅ 新录音停止后把 `sessionId + transcriptSegments + speakerId` 写入 Workspace capture，并后台上传音频得到跨设备 `mediaUrl`。
- ✅ 登录迁移旧课堂时上传完整可恢复包：转录分段、困惑锚点、课堂摘要、精选片段、个人笔记；不再只有数量与 8000 字拼接文本。
- ✅ 新设备下行时按 `sessionId` 逐类回填 IndexedDB，不覆盖本机已有编辑；同一页面生命周期后来到达的新 capture 仍可继续回填，单条失败可重试。
- ✅ 兼容旧 `workspace-account-v1`：识别 `localSessionId`，至少把已有 `normalizedText` 恢复为可阅读、可追问的课堂，不再显示空卡片。
- ✅ 高频转录正规化为 `WorkspaceTranscriptSegment`；锚点、摘要、精选片段、笔记以 `WorkspaceCaptureArtifact(kind, artifactKey, payloadJson)` 保存，避免为低频异构产物制造多套重复表。
- ✅ `/api/workspace/current` 只返回 `evidenceAvailable` 等轻量索引；`/api/workspace/captures/[captureId]/evidence` 在用户打开课堂时按需读取，随后写回 IndexedDB。
- ✅ 进入课堂时静默重试尚未上传的本地原声；视频跨设备恢复保留 `video-link/video-file` 身份、embed/provider/thumbnail/sourceMode。
- ⏳ 后续增强：多设备同时编辑的冲突合并、课堂对话历史同步、对象存储/CDN 生命周期治理。这些不再阻断“另一设备打开完整课堂”的主旅程。

---

## 历史缺口

**原现象**：浏览器 A 上录的课，浏览器 B 登录后看到了卡片，但点开课堂详情看不到转录、找不到锚点、没有 highlight。

**当时根因（已用代码确认，现已修复）**：

1. **服务端只存了汇总文本，不存分段转录**
   - `WorkspaceCapture` 表只有 `normalizedText`（最多 8000 字符截断）和 `tutorContext`
   - 分段 `transcripts` / `anchors` / `summaries` / `highlights` / `notes` / `conversations` **没有对应的 SQLite 表**
   - 当前 `metadataJson` 里**有时**会有 `transcriptSegments`（视频导入路径塞进去的），但 `migrateLocalWorkspaceData` 这条路**没塞**

2. **客户端拉回时只重建 sourceItem，不回填 IndexedDB**
   - `useWorkspaceContextLoader.ts` Effect 1 拿到 captures 后只 push 进 `sourceItems`
   - 没有 `db.audioSessions.put` / `db.transcripts.bulkPut` / `db.anchors.bulkPut` 这种回写
   - 课堂详情、Tutor、Review 全部从 `db.transcripts.where('sessionId').equals(...)` 读 → 浏览器 B 上是空

3. **"双向同步"只做了 A→服务端的"上行"，没做服务端→B 的"下行"完整化**

---

## 已落地方案

### 选项 A：完整双向同步（已采用混合正规化）

**Schema 改造**（`prisma/schema.prisma`）：
```prisma
model WorkspaceTranscriptSegment {
  id        String   @id @default(cuid())
  captureId String
  sessionId String   // 对应 IndexedDB 的 sessionId，跨设备一致
  startMs   Int
  endMs     Int
  text      String
  speakerId String?
  confidence Float?
  isFinal   Boolean
  createdAt DateTime @default(now())
  capture   WorkspaceCapture @relation(fields: [captureId], references: [id], onDelete: Cascade)
  @@index([captureId])
  @@index([sessionId])
}

model WorkspaceCaptureArtifact {
  kind        String // anchor / summary / highlight / note
  artifactKey String
  payloadJson String
}
```

**服务端写入**（`workspace-account-service.migrateLocalWorkspaceData`）：
- 在保存 capture 时，同步 bulk insert 上述子表
- 子表替换使用事务；先暂存完整 metadata，证据落表成功后再收敛为轻索引，失败时仍有完整 metadata 可恢复
- 每节课最多同步 1200 个 transcript segment，覆盖当前课堂时长上限并限制单次写入

**服务端读取**：
- 默认列表只返回轻量 capture（避免回拉全量打死）
- 新增 `/api/workspace/captures/[captureId]/evidence`：用户点开课堂时按需拉完整证据
- 拉回来后写 IndexedDB

**客户端回填**：
- `useWorkspaceContextLoader` 继续兼容旧列表内嵌 bundle
- 新列表只带索引；`openReviewFromCollection` 本地恢复失败后调用 `workspace-evidence-client.ts` 按需拉取
- 拉回数据复用 `backfill-captures-to-indexeddb.ts`，逐类补缺且不覆盖当前设备已有编辑

**结果**：列表负载与课堂长度解耦；旧 bundle 兼容读取，新写入不再重复携带大数组。

### 选项 B：metadata 内塞 transcript（短期妥协）

把 `transcriptSegments` 数组塞进 `WorkspaceCapture.metadataJson`：
- ✅ 不动 schema
- ❌ JSON 字段无法索引、超大（一节课的 transcript 可能 100KB+）、单条记录会膨胀
- ❌ 触发 SQLite 单字段读写性能下降
- ❌ 是技术债，半年内必然要拆出来

**不推荐**。

### 选项 C：明确告知用户"跨设备只看摘要"（最简）

- 不做架构改造
- UI 上：浏览器 B 打开 capture 时，如果本地没有对应 sessionId 的 transcript，显示"这节课在另一台设备上录制，仅可看摘要。在原设备打开可看完整转录"
- ✅ 0 工作量
- ❌ 体验割裂，与"长记忆系统"产品定位冲突

---

## 实施结果

已完成选项 A，并保留选项 B 旧数据的只读兼容。用户无需理解同步机制，也不会在新设备看到空课堂。

---

## 关联代码位置（供下一轮 agent 直接进入）

- 服务端入口：`src/lib/services/workspace-account-service.ts:144` → `migrateLocalWorkspaceData`
- 服务端 Capture 写入：`src/lib/services/workspace-context-service.ts:451` 附近 → `metadataJson` 拼装
- 服务端证据：`src/lib/services/workspace-evidence-service.ts`
- 按需 API：`src/app/api/workspace/captures/[captureId]/evidence/route.ts`
- 客户端懒拉：`src/lib/services/workspace-evidence-client.ts` + `src/app/(main)/app/page.tsx` 的 `openReviewFromCollection`
- 旧 bundle 兼容拉回：`src/hooks/useWorkspaceContextLoader.ts` → Effect 1
- IndexedDB 读路径：`src/hooks/useTutorLauncher.ts:380`、`src/hooks/useAudioSessions.ts:76`、`src/app/(main)/app/matrix/[appKey]/page.tsx:191`
- IndexedDB 写路径：`src/lib/db/sessions.ts`、`src/lib/services/classroom-data-service.ts`
- Schema：`prisma/schema.prisma:106` → `model WorkspaceCapture`
- 本地 IndexedDB schema：`src/lib/db/`

---

## 今天已经做的（不重复）

- ✅ `route.ts`：加了 8MB body 上限、50 sessions 上限、JSON 解析失败明确返回 400、详细日志
- ✅ `useAuth.tsx`：分批推送（每批 20 sessions，413 时自动降级到 5），单批失败不阻断后续，整体失败下次登录重试

这两个保护仍然保留；其后的正规化证据表、按课堂懒拉、IndexedDB 回填与原声补传已经补齐跨设备课堂主链路。
