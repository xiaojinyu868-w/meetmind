# MeetMind 开发路线图

> MVP 阶段规划和 TODO LIST

---

## MVP 阶段规划

### MVP 1.0（4-8周可落地试点）✅ 已完成

**目标**：验证"当天补懂"闭环，家长能"看完就会做动作"

| 功能 | 状态 | 说明 |
|------|:----:|------|
| 纯语音时间轴 | ✅ | 已实现，支持自动分段 |
| 课桌按键 | ⚠️ | 软件实现，需硬件方案 |
| 断点回放 | ✅ | 证据链输出已优化 |
| 学生问答 | ✅ | 支持卡点类型选择 |
| 家长报告 | ✅ | 概览+时间线+播放器+AI总结 |
| 行动清单 | ✅ | 3步补懂路径已优化 |
| **数据分析** | ✅ | 轻量级无感知采集 |

**过线标准**：
- 家长行动清单转化率 > 60%
- 学校/老师愿意持续让你录（留存/续用）

---

### MVP 2.7.4（当前版本）🚀 2026-02-13

**目标**：修复复习闭环关键体验，统一移动端与桌面端能力

| 功能 | 状态 | 说明 |
|------|:----:|------|
| 时间戳 seek 归一 | ✅ | 统一 `number/mm:ss/hh:mm:ss` 时间值解析，修复“回放 XX:XX 跳到 0:00” |
| B站 iframe 同步增强 | ✅ | 优化 embed 参数与 seek/play nonce 机制，提升“所点即所播”一致性 |
| 应用矩阵工作台自适应 | ✅ | 容器级响应式 + 左栏拖拽上限从 450 提升到 820，解决内容显示不全 |
| 移动端应用矩阵入口 | ✅ | 移动端补齐应用矩阵菜单入口与页面，复习模式可直接进入工作台 |
| 移动端回放行为统一 | ✅ | 精选/摘要/笔记统一走 `handleUnifiedSeek`，与桌面端回放一致 |
| 移动端录音入口 UI 精修 | ✅ | 输入方式切换改为单行胶囊导航，避免标签跨两行 |
| 视频导入面板视觉重构 | ✅ | 重做移动端导入卡片层级、模式区与主按钮，提升可读性与质感 |
| 工作区状态持久化抽离 | ✅ | 新增 `src/lib/services/app-workspace-state.ts`，支持刷新/切页后恢复视图与上下文 |
| 录音输入面板去重 | ✅ | 录音页输入入口改为统一渲染逻辑，降低双端分支维护成本 |
| 最小闭环自动化回归 | ✅ | 新增 Playwright 用例 `tests/e2e/closed-loop.spec.ts`，覆盖核心闭环场景 |
| 公网部署长任务稳定性 | ✅ | `nginx-hk.conf` 增加视频导入/转录接口长超时配置，减少长视频任务超时中断 |

### MVP 2.7.3（已完成）✅ 2026-02-11

**目标**：录音体验优化

| 功能 | 状态 | 说明 |
|------|:----:|------|
| 切换 Tab 保持录音状态 | ✅ | Recorder 组件改为 CSS 隐藏而非卸载，切换 Tab 不丢失状态（v2.7.3 新增） |
| 暂停真正停止转录 | ✅ | 暂停时挂起 AudioContext，彻底停止向 ASR 发送音频（v2.7.3 新增） |
| 实时录音划词提问 | ✅ | 录音过程中选中文本即可弹出 WordExplainer 进行提问（v2.7.3 新增） |
| 麦克风回声消除默认关闭 | ✅ | echoCancellation 默认关闭，麦克风可录到电脑外放声音（v2.7.3 新增） |

### MVP 2.7.2（已完成）✅ 2026-02-11

**目标**：视频字幕高亮与历史会话恢复

| 功能 | 状态 | 说明 |
|------|:----:|------|
| 视频字幕荧光笔高亮 | ✅ | B站/YouTube 视频播放时，转录文本实时高亮当前片段（v2.7.2 新增） |
| IframeSyncBar 控制条 | ✅ | 新增字幕同步控制条，支持播放/暂停/拖拽/倍速（v2.7.2 新增） |
| 录音历史视频恢复 | ✅ | 从历史点击 VIDEO 会话正确恢复视频播放器（v2.7.2 新增） |
| TranscriptFlowView 组件 | ✅ | 统一转录文本显示组件，支持四种模式（v2.7.2 新增） |

### MVP 2.7.1（已完成）✅ 2026-02-10

**目标**：B站视频导入稳定性修复

| 功能 | 状态 | 说明 |
|------|:----:|------|
| ffmpeg 崩溃修复 | ✅ | 修复 `ffmpeg-static` 二进制损坏导致的转码崩溃，优先使用系统 ffmpeg |
| 音频码率优化 | ✅ | 选择最低码率音频流，降低下载大小和内存占用 |
| Cookie 配置简化 | ✅ | 移除主动弹出的复杂引导，失败时提示，教程折叠展示 |
| 内存优化 | ✅ | ffmpeg 限制单线程、限制 muxing 缓冲区 |
| 限流移除 | ✅ | 视频导入不再受 `transcribe` 限流影响 |

### MVP 2.7（已完成）✅

**目标**：AI 交互增强，流式输出体验与选词解释

| 功能 | 状态 | 说明 |
|------|:----:|------|
| AIChat 流式输出 | ✅ | 从一次性请求升级为 SSE 流式输出（v2.7 新增） |
| 思维引导开关 | ✅ | AI 对话支持开启/关闭思考过程可视化（v2.7 新增） |
| 选词解释功能 | ✅ | 转录文本中选词即弹出 AI 解释浮窗（v2.7 新增） |
| WordExplainer 浮窗 | ✅ | 支持拖拽移动、四边/四角缩放、语音输入、图片上传/粘贴（v2.7 新增） |
| 编辑交互优化 | ✅ | TranscriptPreviewPanel/TimelineView 编辑改为双击触发（v2.7 新增） |
| B站视频导入 ASR 回退 | ✅ | 改善视频导入场景下的语音识别回退策略（v2.7 新增） |

### MVP 2.5（已完成）✅

**目标**：多端适配优化，提升用户体验

| 功能 | 状态 | 说明 |
|------|:----:|------|
| 自适应布局 | ✅ | 100dvh 动态视口、安全区域支持（v2.5 新增） |
| AI 对话框优化 | ✅ | 根据屏幕高度动态调整最小高度（v2.5 新增） |
| 浏览器兼容性 | ✅ | 修复联想 Edge 等浏览器标签遮挡问题（v2.5 新增） |
| Viewport 配置 | ✅ | 优化移动端缩放和主题色（v2.5 新增） |
| Nginx 性能优化 | ✅ | HTTP/2、Gzip、TLS 1.3、TCP BBR（v2.5 新增） |

### MVP 2.1（已完成）✅

**目标**：完善核心功能，增强 AI 交互体验

| 功能 | 状态 | 说明 |
|------|:----:|------|
| API 配额系统 | ✅ | 用户级别配额管理（v2.1 新增） |
| 限流服务 | ✅ | Redis/内存双模式（v2.1 优化） |
| 对话历史持久化 | ✅ | IndexedDB 存储，支持续聊（v2.1 新增） |
| 思维可视化 | ✅ | AI 推理过程展示（v2.1 新增） |
| 转录增强 API | ✅ | 多模式转录支持（v2.1 新增） |
| 教师反思面板 | ✅ | AI 流式生成，已接入真实数据 |

---

### MVP 2.8（下一版本）

**目标**：学生在"最小有效训练集 + 对话辅导"下，显著提升掌握修复率

| 功能 | 状态 | 说明 |
|------|:----:|------|
| 课件文本对齐 | ❌ | 支持课件导入/电子教案同步 |
| 讲台/黑板关键帧 | ❌ | 事件触发截帧 |
| 少题高命中练习 | ❌ | "3题补懂"个性化推荐 |

**过线标准**：
- 当日掌握修复率 > 80%
- 复习时间效率提升 > 30%

---

### MVP 3.0（6-12个月）

**目标**：数据飞轮形成，可规模化部署

| 功能 | 状态 | 说明 |
|------|:----:|------|
| 学期级记忆 | ❌ | 跨课迁移、长期知识图谱 |
| 分层教学建议 | ❌ | 基于班级数据优化 |
| 学校级质量诊断 | ❌ | 运维面板、合规管理 |

**过线标准**：
- 断点定位更准、教师端反馈更有用
- 合规与接受度不下降

---

## TODO LIST

### 🔴 高优先级（v2.7.2 已完成 ✅）

#### 视频字幕高亮与同步（v2.7.2 新增）

- [x] **视频字幕荧光笔高亮**
  - 文件：`src/components/TranscriptFlowView.tsx`
  - ✅ 已实现：B站/YouTube 视频播放时，下方转录文本实时高亮当前播放片段
  - 视觉状态：已播放（灰色）、正在播放（黄色荧光笔）、未播放（正常色）
  - 自动滚动：活跃段落自动滚动到可视区域

- [x] **IframeSyncBar 字幕同步控制条**
  - 文件：`src/components/VideoReviewPlayer.tsx`
  - ✅ 已实现：解决 iframe 跨域无法获取播放进度的问题
  - 功能：播放/暂停按钮、可拖拽进度条、倍速切换（0.5x-2x）、时间显示

- [x] **录音历史视频会话恢复**
  - 文件：`src/app/(main)/app/page.tsx` - `handleLoadHistorySession`
  - ✅ 已实现：从历史点击 VIDEO 类型会话，正确恢复 `videoSource` 和 `dataSource`
  - 修复：之前无论什么类型都进入普通音频复习模式

### 🔴 高优先级（v2.7 已完成 ✅）

#### AI 交互增强（v2.7 新增）

- [x] **AIChat 流式输出升级**
  - 文件：`src/components/AIChat.tsx`
  - ✅ 已实现：从 `stream: false` 一次性请求改为 `useSimpleSSEStream` SSE 流式输出
  - 新增：思维引导开关（🧠 复选框）、思考过程可视化、停止生成按钮、流式 Markdown 渲染

- [x] **选词解释功能**
  - 文件：`src/hooks/useTextSelection.ts`、`src/components/WordExplainer.tsx`
  - ✅ 已实现：转录文本中选中词汇后弹出「解释一下」浮窗，AI 结合课堂上下文流式解释
  - 功能：拖拽移动、四边/四角缩放、语音输入（VoiceMicButton）、图片上传/粘贴、模型切换、多轮追问

- [x] **选词解释接入转录面板**
  - 文件：`src/components/TranscriptPreviewPanel.tsx`、`src/components/TimelineView.tsx`、`src/app/(main)/app/page.tsx`
  - ✅ 已实现：新增 `enableWordExplainer`、`fullContextText` props，在视频导入的转录面板和复习模式中启用

- [x] **编辑交互优化**
  - 文件：`src/components/TranscriptPreviewPanel.tsx`、`src/components/TimelineView.tsx`
  - ✅ 已实现：转录文本编辑从单击触发改为双击触发，避免与选词解释的文本选择冲突

### 🔴 高优先级（v2.5 已完成 ✅）

#### 自适应布局与多端体验优化（v2.5 新增）

- [x] **动态视口高度**
  - 文件：`src/app/globals.css`、`src/app/layout.tsx`
  - ✅ 已实现：使用 `100dvh` 替代 `100vh`，适配移动端浏览器工具栏

- [x] **安全区域支持**
  - 文件：`src/app/globals.css`、`src/app/layout.tsx`
  - ✅ 已实现：`safe-area-inset` CSS 变量，适配刘海屏/挖孔屏

- [x] **AI 对话框高度优化**
  - 文件：`src/app/(main)/app/page.tsx`、`src/components/AITutor.tsx`
  - ✅ 已实现：根据屏幕高度动态设置最小高度（200px-350px）

- [x] **标签按钮防遮挡**
  - 文件：`src/app/(main)/app/page.tsx`
  - ✅ 已实现：增加 z-index、点击区域、浏览器安全边距

- [x] **Viewport 配置优化**
  - 文件：`src/app/layout.tsx`
  - ✅ 已实现：`viewportFit: 'cover'`、主题色、可缩放设置

#### API 配额与对话历史系统（v2.1 已完成 ✅）

#### API 配额与限流系统（v2.1 新增）

- [x] **API 使用配额**
  - 文件：`prisma/schema.prisma`、`src/lib/services/quota-service.ts`
  - ✅ 已实现：ApiUsage 模型，记录用户 API 调用配额（转录、AI 摘要、AI 家教等）

- [x] **访客配额支持**
  - 文件：`src/lib/services/quota-service.ts`
  - ✅ 已实现：支持 `guest_xxx` 形式的访客用户配额追踪

- [x] **限流服务优化**
  - 文件：`src/lib/services/rate-limit-service.ts`
  - ✅ 已实现：移除无效的 Redis 配置选项，提升服务稳定性

#### 对话历史系统（v2.1 新增）

- [x] **对话历史持久化**
  - 文件：`src/lib/db/conversations.ts`、`src/types/conversation.ts`
  - ✅ 已实现：AI 家教对话记录保存到 IndexedDB，支持历史查询

- [x] **对话历史 Hook**
  - 文件：`src/hooks/useConversationHistory.ts`
  - ✅ 已实现：对话记录查询、保存、删除

- [x] **对话历史组件**
  - 文件：`src/components/ConversationHistory/`
  - ✅ 已实现：历史对话列表展示、续聊功能

#### 思维可视化（v2.1 新增）

- [x] **AI 推理过程可视化**
  - 文件：`src/components/ThinkingVisualizer.tsx`、`src/components/ThinkingGuideRenderer.tsx`
  - ✅ 已实现：展示 AI 的推理过程，引导学生思考

#### 数据分析系统（v2.0 已完成 ✅）

- [x] **轻量级数据采集**
  - 文件：`src/lib/services/analytics-service.ts`
  - ✅ 已实现：会话管理、页面追踪、事件追踪、统计查询

- [x] **Prisma 数据模型**
  - 文件：`prisma/schema.prisma`
  - ✅ 已实现：UserAnalytics、PageView、EventTrack 三个模型

- [x] **客户端采集 Hook**
  - 文件：`src/hooks/useAnalytics.ts`
  - ✅ 已实现：心跳机制、页面可见性追踪、sendBeacon 保证数据不丢失

- [x] **分析上下文提供者**
  - 文件：`src/components/AnalyticsProvider.tsx`
  - ✅ 已实现：全局会话管理、核心事件追踪、用户登录状态同步

- [x] **数据上报 API**
  - 文件：`src/app/api/analytics/route.ts`
  - ✅ 已实现：session_start/update/end、page_view、event、batch

- [x] **统计查询 API**
  - 文件：`src/app/api/analytics/stats/route.ts`
  - ✅ 已实现：overview、trend、ip 三种查询类型

#### 登录页优化 & 新用户引导系统（v1.8-v1.9）

- [x] **登录页 UI 优化**：毛玻璃透明效果
  - 文件：`src/app/(auth)/login/page.tsx`
  - ✅ 已实现：登录卡片增加 `backdrop-blur` 毛玻璃效果，更现代的视觉体验

- [x] **产品标语更新**
  - 文件：`src/app/(auth)/login/page.tsx`
  - ✅ 已实现："AI智能学习助手 - 你的专属AI同桌"

- [x] **未设置密码处理**
  - 文件：`src/app/(auth)/login/page.tsx`
  - ✅ 已实现：自动切换到验证码登录，显示友好提示

- [x] **手机号登录暂停**
  - 文件：`src/app/(auth)/login/page.tsx`
  - ✅ 已实现：标记为"即将开放"，按钮置灰

- [x] **新用户引导系统**
  - 文件：`src/hooks/useOnboarding.ts`、`src/components/OnboardingGuide.tsx`
  - ✅ 已实现：首次访问显示欢迎弹窗 + 3 步引导教程
  - 功能：聚光灯高亮（SVG mask）、气泡提示、步骤指示器
  - 状态：IndexedDB 持久化，支持跳过/完成/重新触发

- [x] **首次访问路由优化**
  - 文件：`src/app/(main)/app/page.tsx`
  - ✅ 已实现：首次访问强制进入录音页面（而非复习页面）

- [x] **交互式引导模式**（v1.9 新增）
  - 文件：`src/components/OnboardingGuide.tsx`、`src/hooks/useOnboarding.ts`
  - ✅ 已实现：用户点击目标元素进入下一步，而非点击"下一步"按钮
  - ✅ 已实现：步骤级 `interactive` 配置，控制是否触发真实功能
  - ✅ 已实现：遮罩点击不退出，避免误触

- [x] **移动端引导适配**（v1.9 新增）
  - 文件：`src/components/mobile/MobileAIFab.tsx`
  - ✅ 已实现：针对移动端的引导流程（AI FAB + 菜单按钮）

#### 性能与架构优化

- [x] **SWR 数据层**：统一数据获取和缓存策略
  - 文件：`src/lib/swr/`、`src/hooks/data/`
  - ✅ 已实现：fetcher、provider、5 个数据 Hooks

- [x] **Barrel File 优化**：改为直接导入
  - 文件：`src/app/page.tsx`
  - ✅ 已实现：提升 tree-shaking 效果

- [x] **Zustand 状态管理基础设施**
  - 文件：`src/stores/ui-store.ts`、`src/stores/player-store.ts`
  - ✅ 已实现：为后续状态迁移做准备

- [ ] **完成状态迁移**：将 page.tsx 的 25+ useState 迁移到 Zustand
  - 文件：`src/app/page.tsx`、`src/stores/`
  - 预期：减少 page.tsx 代码量 30-40%

- [x] **可访问性增强**
  - 文件：`src/components/ActionDrawer.tsx`、`src/components/mobile/MiniPlayer.tsx`
  - ✅ 已实现：ARIA 属性、键盘导航、focus-visible、prefers-reduced-motion

#### 学生端优化

- [ ] **证据链输出优化**：AI 回答必须带来源分钟引用
  - 文件：`src/lib/services/tutor-service.ts`
  - 要求：每个解释都引用 `[xx:xx-xx:xx]` 格式的课堂片段
  - ✅ 已实现：时间戳可点击跳转播放

- [x] **卡点类型一键选择**：提供 4 类快捷按钮
  - 文件：`src/components/AITutor.tsx`
  - ✅ 已实现：选择题模式，根据录音内容自动生成精准选项

- [ ] **补懂闭环四步**：回放 → 定位 → 解释 → 确认
  - 文件：`src/components/AITutor.tsx`
  - 要求：完成后标记"已补懂✅"或"仍需复听🔁"

#### 家长端（已完成基础版本 ✅）

- [x] **今日概览卡片**：课程数 / 困惑数 / 已解决数
  - 文件：`src/components/parent/TodayOverview.tsx`

- [x] **困惑时间线**：按科目分类、待解决/已解决分组
  - 文件：`src/components/parent/ConfusionTimeline.tsx`
  - ✅ 已实现：学科颜色标签、展开详情、标记已解决

- [x] **老师原话播放器**：点击困惑点可播放对应片段
  - 文件：`src/components/parent/TeacherAudioPlayer.tsx`

- [x] **AI 总结卡片**：自动生成今日学情总结
  - 文件：`src/components/parent/AISummaryCard.tsx`

- [ ] **接入真实数据**：替换演示数据，读取学生端 IndexedDB
  - 文件：`src/lib/services/parent-service.ts`

#### 教师端增强

- [x] **困惑热点 TOP3**：按分钟段聚合按键分布
  - 文件：`src/components/teacher/ConfusionHotspotCard.tsx`
  - ✅ 已实现：金银铜排名、热度指示条、可展开学生列表

- [x] **一键生成课后反思**：AI 流式生成
  - 文件：`src/components/teacher/ReflectionGenerator.tsx`
  - ✅ 已实现：接入通义千问、流式输出、支持停止/重新生成
  - 模板：课堂总结 / 教学亮点 / 问题分析 / 改进建议

---

### 🟡 中优先级（MVP 2.0）

#### 多模态增强

- [ ] **课件文本对齐**：导入课件/电子教案
  - 新增服务：`src/lib/services/courseware-service.ts`

- [ ] **讲台/黑板关键帧**：事件触发截帧
  - 触发条件：按键触发前后各 10 秒
  - 合规要求：只拍讲台/黑板，不拍学生

#### 练习推荐

- [ ] **少题高命中练习**：基于困惑点生成变式题
  - 新增服务：`src/lib/services/exercise-service.ts`
  - 要求：每题标注"为什么是这题"

---

### 🟢 低优先级（MVP 3.0）

- [ ] 学期级记忆与跨课迁移
- [ ] 分层教学建议
- [ ] 学校级运维面板
- [ ] 合规与数据安全管理

---

## 相关文档

- [项目结构.md](./项目结构.md) - 目录结构和文件说明
- [技术架构.md](./技术架构.md) - 技术栈、系统架构、数据流
- [API接口文档.md](./API接口文档.md) - API 路由和核心组件说明
- [AI原生应用矩阵实施方案.md](./AI原生应用矩阵实施方案.md) - 应用矩阵 SSOT（策略、协议、分层与落地进展）
- [异步开发分工方案.md](./异步开发分工方案.md) - Git 分支与团队协作规范

---

*文档版本：v2.7.4*
*更新日期：2026-02-13*
*更新内容：新增 v2.7.4（时间戳 seek 归一、B站同步增强、应用矩阵自适应、移动端能力同步与 UI 精修）；将 v2.7.3 标记为已完成*
