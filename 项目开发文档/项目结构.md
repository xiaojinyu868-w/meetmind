# MeetMind 项目结构

> 详细的目录结构和文件说明

---

## 目录结构

```
meetmind/
├── server.js                    # 自定义服务器 (WebSocket ASR 代理 + Next.js)
├── package.json                 # 项目依赖
├── .eslintrc.json               # ESLint 规则（非交互 lint 基线）
├── .env.example                 # 环境变量示例
├── next.config.js               # Next.js 配置
├── tailwind.config.js           # Tailwind CSS 配置
├── tsconfig.json                # TypeScript 配置
├── docker-compose.yml           # Docker 部署配置（Redis 等）
│
├── prisma/                      # Prisma ORM
│   ├── schema.prisma            # 数据库模型定义（包含 ApiUsage 模型）
│   └── meetmind.db              # SQLite 数据库文件
│
├── public/                      # 静态资源
│   ├── demo.mp3                 # 演示音频
│   └── temp-audio/              # 临时音频目录（异步转录用，自动清理）
│
└── src/
    ├── middleware.ts            # Next.js 中间件 (认证、路由控制)
    │
    ├── app/                     # Next.js App Router 页面
    │   ├── layout.tsx           # 根布局 (AuthProvider + AnalyticsProvider)
    │   ├── page.tsx             # 学生端首页 (录音/复习)
    │   ├── globals.css          # 全局样式
    │   ├── error.tsx            # 错误页面
    │   ├── not-found.tsx        # 404 页面
    │   │
    │   ├── (auth)/              # 认证相关页面组
    │   │   ├── login/page.tsx   # 登录页
    │   │   ├── register/page.tsx# 注册页
    │   │   ├── settings/page.tsx# 设置页
    │   │   └── profile/page.tsx # 个人中心
    │   │
    │   ├── (main)/              # 主应用页面组
    │   │   └── app/page.tsx     # 应用主页
    │   │
    │   ├── parent/              # 家长端
    │   │   └── page.tsx         # 日报查看、陪学脚本
    │   │
    │   ├── teacher/             # 教师端
    │   │   └── page.tsx         # 困惑热点、教学反思
    │   │
    │   ├── all-notes/           # 全部笔记页面
    │   │   └── page.tsx
    │   │
    │   ├── feedback/            # 用户反馈页面
    │   │   └── page.tsx
    │   │
    │   ├── help/                # 帮助页面
    │   │   └── page.tsx
    │   │
    │   └── api/                 # API 路由 (13个模块)
    │       ├── analytics/       # 数据分析 API
    │       │   ├── route.ts     # 数据上报接口
    │       │   └── stats/route.ts # 统计查询接口
    │       ├── auth/            # 认证 API (12个接口)
    │       │   ├── login/route.ts
    │       │   ├── register/route.ts
    │       │   ├── refresh/route.ts
    │       │   ├── verify-code/route.ts
    │       │   ├── send-code/route.ts
    │       │   └── ...
    │       ├── tutor/route.ts   # AI 家教核心 API
    │       ├── chat/route.ts    # 通用 AI 对话
    │       ├── feedback/route.ts# 用户反馈 API
    │       ├── transcribe/      # 离线语音转录 (阿里云异步 ASR)
    │       ├── transcribe-fast/ # 快速转录 API
    │       ├── transcribe-turbo/# Turbo 转录 API
    │       ├── transcript-enhance/ # 转录文本增强
    │       ├── generate-topics/ # 精选片段生成
    │       ├── generate-summary/# 课堂摘要生成
    │       ├── upload-audio/    # 音频上传
    │       └── asr-config/      # ASR 配置
    │
    ├── components/              # React 组件 (100+)
    │   ├── index.ts             # 统一导出
    │   ├── AnalyticsProvider.tsx# 数据分析上下文
    │   │
    │   ├── ui/                  # 基础 UI 组件 (shadcn/ui, 24个)
    │   │   ├── button.tsx
    │   │   ├── card.tsx
    │   │   ├── dialog.tsx
    │   │   ├── input.tsx
    │   │   ├── select.tsx
    │   │   ├── tabs.tsx
    │   │   ├── scroll-area.tsx
    │   │   ├── tooltip.tsx
    │   │   └── ...
    │   │
    │   ├── mobile/              # 移动端专用组件 (14个)
    │   │   ├── MobileRecorder.tsx      # 移动端录音器
    │   │   ├── MobileReviewPanel.tsx   # 移动端复习面板
    │   │   ├── MobileAITutor.tsx       # 移动端 AI 家教
    │   │   ├── MobileAIFab.tsx         # AI 悬浮按钮
    │   │   ├── MiniPlayer.tsx          # 迷你播放器
    │   │   └── ...
    │   │
    │   ├── teacher/             # 教师端专用组件
    │   │   ├── TeacherDashboard.tsx     # 教师仪表盘
    │   │   ├── ConfusionHotspotCard.tsx # 困惑热点卡片
    │   │   └── ReflectionGenerator.tsx  # 教学反思生成器
    │   │
    │   ├── parent/              # 家长端专用组件
    │   │
    │   ├── business/            # 业务组件
    │   │   ├── LearningCard.tsx        # 学习卡片
    │   │   ├── ProgressRing.tsx        # 进度环
    │   │   └── ...
    │   │
    │   ├── layout/              # 布局组件
    │   │
    │   ├── ConversationHistory/ # 对话历史组件
    │   │
    │   │   # 核心功能组件
    │   ├── Recorder.tsx         # 录音器 (实时 ASR)
    │   ├── AudioPlayer.tsx      # 音频播放器
    │   ├── AudioUploader.tsx    # 音频上传器
    │   ├── WaveformPlayer.tsx   # 波形播放器 (WaveSurfer)
    │   │
    │   │   # AI 交互组件
    │   ├── AITutor.tsx          # AI 家教面板
    │   ├── AIChat.tsx           # AI 对话组件（SSE 流式输出 + 思维引导）
    │   ├── ModelSelector.tsx    # 模型选择器
    │   ├── ThinkingVisualizer.tsx # 思维过程可视化
    │   ├── ThinkingGuideRenderer.tsx # 思维引导渲染
    │   ├── WordExplainer.tsx    # 选词解释浮窗（拖拽/缩放/语音/图片/追问）
    │   ├── VoiceMicButton.tsx   # 语音输入按钮
    │   ├── ImageUpload.tsx      # 图片上传组件（支持粘贴）
    │   │
    │   │   # 时间轴与内容组件
    │   ├── TimelineView.tsx     # 时间轴视图
    │   ├── HighlightsPanel.tsx  # 精选片段面板
    │   ├── SummaryPanel.tsx     # 课堂摘要面板
    │   ├── NotesPanel.tsx       # 笔记面板
    │   ├── TranscriptPreviewPanel.tsx # 转录预览面板
    │   ├── TranscriptFlowView.tsx     # 统一转录文本显示组件（v2.7.2 新增，支持荧光笔高亮）
    │   │
    │   │   # 可视化组件
    │   ├── ConfusionHeatmap.tsx # 困惑热力图
    │   │
    │   │   # 布局组件
    │   ├── Header.tsx           # 页头导航
    │   ├── ActionList.tsx       # 行动清单
    │   ├── ActionDrawer.tsx     # 行动抽屉
    │   ├── ActionSidebar.tsx    # 行动侧边栏
    │   └── OnboardingGuide.tsx  # 新用户引导组件
    │
    ├── hooks/                   # 自定义 Hooks (21个)
    │   ├── index.ts             # 统一导出
    │   ├── useAnalytics.ts      # 数据分析 Hook：自动追踪页面停留时长、心跳机制
    │   ├── useAnchors.ts        # 困惑点 CRUD 操作、状态管理
    │   ├── useAudio.ts          # 音频播放控制（播放/暂停/跳转/倍速/音量）
    │   ├── useAudioSessions.ts  # IndexedDB 会话查询（Dexie 响应式）
    │   ├── useConversationHistory.ts # 对话历史管理
    │   ├── useDragGesture.ts    # 拖拽手势处理
    │   ├── useNetworkStatus.ts  # 网络状态检测（v2.7.3 新增）
    │   ├── useOnboarding.ts     # 新用户引导状态管理
    │   ├── useRecording.ts      # 录音状态管理（开始/停止/暂停/恢复）
    │   ├── useResizable.ts      # 可调整大小
    │   ├── useResponsive.ts     # 响应式布局状态检测
    │   ├── useTextSelection.ts  # 文本选择监听（选词解释用，v2.7 新增）
    │   ├── useTranscript.ts     # 转录数据加载/保存/实时更新
    │   ├── useVoiceInput.ts     # 语音输入 Hook（v2.7 新增）
    │   │
    │   └── data/                # SWR 数据 Hooks
    │       ├── useTopics.ts     # 精选片段数据
    │       ├── useSummary.ts    # 摘要数据
    │       ├── useTutor.ts      # AI 家教数据
    │       ├── useSession.ts    # 会话管理
    │       └── useTranscript.ts # 转录数据
    │
    ├── stores/                  # Zustand 状态管理
    │   ├── ui-store.ts          # UI 状态 (drawer/modal/toast)
    │   └── player-store.ts      # 播放器状态 (播放/音量/列表)
    │
    ├── lib/                     # 核心库
    │   ├── prisma.ts            # Prisma Client 实例
    │   ├── db.ts                # 兼容层（重导出 db/index.ts）
    │   │
    │   ├── db/                  # IndexedDB 数据库模块
    │   │   ├── index.ts         # 统一导出
    │   │   ├── schema.ts        # 数据库定义 + 类型
    │   │   ├── sessions.ts      # AudioSession 操作
    │   │   ├── anchors.ts       # Anchor 操作
    │   │   ├── transcripts.ts   # TranscriptSegment 操作
    │   │   ├── lexicon.ts       # TranscriptLexiconEntry 操作（v2.7.3 新增，转录词典）
    │   │   ├── highlights.ts    # HighlightTopic 操作
    │   │   ├── summaries.ts     # ClassSummary 操作
    │   │   ├── notes.ts         # Note 操作
    │   │   ├── conversations.ts # 对话历史操作
    │   │   ├── tutor-cache.ts   # TutorResponseCache 操作
    │   │   └── preferences.ts   # Preference 操作
    │   │
    │   ├── config/              # 统一配置管理
    │   │   ├── app.config.ts    # 应用配置 (LLM/Auth/ASR/Feature/UI)
    │   │   └── index.ts         # 配置导出
    │   │
    │   ├── swr/                 # SWR 数据获取层
    │   │   ├── fetcher.ts       # 统一 fetcher
    │   │   ├── provider.tsx     # SWR 全局 Provider
    │   │   └── index.ts         # 统一导出
    │   │
    │   ├── utils/               # 工具函数
    │   │   ├── index.ts         # 统一导出
    │   │   ├── time-utils.ts    # 时间处理工具
    │   │   ├── json-utils.ts    # JSON 解析工具
    │   │   └── transcript-utils.ts # 转录文本处理
    │   │
    │   ├── services/            # 服务层 (33个服务)
    │   │   ├── index.ts         # 统一导出
    │   │   │
    │   │   │   # 数据分析服务
    │   │   ├── analytics-service.ts   # 会话/页面/事件追踪、统计查询
    │   │   ├── quota-service.ts       # API 配额管理（v2.1 新增）
    │   │   ├── rate-limit-service.ts  # 请求限流服务
    │   │   │
    │   │   │   # AI/LLM 服务
    │   │   ├── llm-service.ts           # LLM 调用封装 (通义千问/Gemini/OpenAI)
    │   │   ├── dify-service.ts          # Dify 工作流服务
    │   │   ├── tutor-service.ts         # AI 家教服务
    │   │   ├── web-search-service.ts    # 联网搜索服务
    │   │   │
    │   │   │   # ASR 语音识别
    │   │   ├── qwen-asr-service.ts      # 通义千问 ASR (同步/异步)
    │   │   ├── dashscope-asr-service.ts # DashScope 实时 ASR 客户端
    │   │   │
    │   │   │   # 媒体处理服务
    │   │   ├── bilibili-import-service.ts # B站视频导入服务（v2.7 新增）
    │   │   ├── media-tooling.ts          # 媒体处理工具（v2.7 新增）
    │   │   │
    │   │   │   # 核心业务服务
    │   │   ├── meetmind-service.ts      # MeetMind 核心业务
    │   │   ├── anchor-service.ts        # 困惑点管理
    │   │   ├── memory-service.ts        # 时间轴记忆服务
    │   │   ├── classroom-data-service.ts# 课堂数据共享
    │   │   ├── conversation-service.ts  # 对话服务
    │   │   │
    │   │   │   # 内容生成服务
    │   │   ├── highlight-service.ts     # 精选片段生成
    │   │   ├── summary-service.ts       # 课堂摘要生成
    │   │   ├── note-service.ts          # 笔记服务
    │   │   ├── transcript-enhancer.ts   # 转录文本增强
    │   │   │
    │   │   │   # 角色专用服务
    │   │   ├── parent-service.ts        # 家长端服务
    │   │   ├── auth-service.ts          # 认证服务
    │   │   ├── wechat-auth-service.ts   # 微信认证服务
    │   │   ├── teaching-suggestion.ts   # 教学建议生成
    │   │   │
    │   │   │   # 工具服务
    │   │   ├── email-service.ts         # 邮件服务
    │   │   ├── sms-service.ts           # 短信服务
    │   │   ├── verification-code-service.ts # 验证码服务
    │   │   ├── search-service.ts        # 搜索服务
    │   │   ├── health-check.ts          # 健康检查
    │   │   ├── longcut-service.ts       # LongCut 服务
    │   │   ├── longcut-utils.ts         # LongCut 工具
    │   │   └── notebook-service.ts      # 笔记本服务
    │   │
    │   └── hooks/               # 认证相关 Hooks
    │       └── useAuth.tsx      # 认证 Hook
    │
    ├── types/                   # TypeScript 类型定义
    │   ├── index.ts             # 核心业务类型
    │   ├── user.ts              # 用户/认证类型
    │   ├── conversation.ts      # 对话类型（v2.1 新增）
    │   ├── dify.ts              # Dify 服务类型
    │   └── onboarding.ts        # 引导系统类型
    │
    └── fixtures/                # 演示数据
        └── demo-data.ts         # 统一演示数据源
```

---

## 核心目录说明

### `/src/app` - 页面路由

使用 Next.js App Router，按文件系统路由组织：

| 路径 | 说明 |
|------|------|
| `/` | 学生端首页（录音/复习模式） |
| `/parent` | 家长端（日报、陪学脚本） |
| `/teacher` | 教师端（困惑热点、教学反思） |
| `/all-notes` | 全部笔记页面 |
| `/(auth)/*` | 认证页面组（登录/注册/个人中心） |
| `/api/*` | API 路由 |

### `/src/components` - React 组件

按功能分类组织：

| 分类 | 组件 |
|------|------|
| **基础 UI** | button, card, dialog, input, select, tabs, ... (shadcn/ui) |
| **音频组件** | Recorder, AudioPlayer, WaveformPlayer |
| **AI 交互** | AITutor, AIChat（流式）, ModelSelector, WordExplainer, VoiceMicButton, ImageUpload |
| **时间轴** | TimelineView, HighlightsPanel, SummaryPanel |
| **可视化** | ConfusionHeatmap |
| **布局** | Header, ActionList |
| **移动端** | MobileRecorder, MobileReviewPanel, MobileAITutor, MiniPlayer, MobileAIFab |
| **教师端** | TeacherDashboard, ConfusionHotspotCard, ReflectionGenerator |
| **业务组件** | LearningCard, ProgressRing |
| **引导系统** | OnboardingGuide, WelcomeModal |

### `/src/hooks` - 自定义 Hooks

**v1.6 重构**：从 `page.tsx` 提取核心业务 Hooks，降低主页面复杂度。

| Hook | 说明 |
|------|------|
| `useAnalytics` | 数据分析 Hook：自动追踪页面停留时长、心跳机制、sendBeacon 上报 |
| `useAnchors` | 困惑点 CRUD 操作、状态管理、`updateAnchorStatus` 方法 |
| `useAudio` | 音频播放控制（播放/暂停/跳转/倍速/音量） |
| `useAudioSessions` | IndexedDB 会话查询（Dexie 响应式 `useLiveQuery`） |
| `useConversationHistory` | 对话历史管理（v2.1 新增）：对话记录查询、保存、删除 |
| `useDragGesture` | 拖拽手势处理（触摸/鼠标） |
| `useOnboarding` | 新用户引导状态管理：引导流程控制、步骤跳转、状态持久化 |
| `useRecording` | 录音状态管理（开始/停止/暂停/恢复） |
| `useResizable` | 可调整大小组件 |
| `useResponsive` | 响应式布局状态检测（移动端/桌面端） |
| `useTextSelection` | 文本选择监听：检测容器内选词事件，返回选中文本和位置（v2.7 新增） |
| `useTranscript` | 转录数据加载/保存/实时更新 |

### `/src/lib/config` - 配置管理

**v1.6 重构**：将散落各处的硬编码配置统一到 `app.config.ts`，敏感信息移至环境变量。

| 配置项 | 说明 |
|--------|------|
| `LLMConfig` | LLM 模型配置（默认模型、API Key、可用模型列表、baseURL） |
| `AuthConfig` | 认证配置（JWT 密钥从环境变量读取、密码策略、登录限流） |
| `ASRConfig` | 语音识别配置（实时模型、异步模型、采样率、分块大小） |
| `FeatureConfig` | 功能配置（精选片段、摘要、家长端、AI 家教开关） |
| `UIConfig` | UI 配置（`defaultSubject` 默认学科、分页大小、动画时长） |
| `DevConfig` | 开发配置（调试模式、演示模式、日志级别） |

### 环境变量配置

| 变量 | 说明 |
|------|------|
| `DASHSCOPE_API_KEY` | 阿里云百炼 API Key（必需） |
| `DASHSCOPE_ASR_WS_MODEL` | 实时语音识别模型（默认 qwen3-asr-flash-realtime） |
| `PUBLIC_HOST` | 公网访问地址（异步转录需要，如 47.112.160.134:3001） |
| `PUBLIC_PROTOCOL` | 公网协议（http/https） |

### `/src/lib/utils` - 工具函数

**重构背景**：v1.6 版本消除了 4 处重复的 `formatTimestamp` 函数定义，统一到模块化工具函数中。

| 工具 | 说明 |
|------|------|
| `time-utils.ts` | 时间格式化（`formatTimestamp`、`formatTimeRange`）、毫秒/秒转换、`TimeSegment` 泛型接口 |
| `json-utils.ts` | 安全 JSON 解析（`safeParseJSON`）、从 Markdown 代码块提取 JSON（`extractJSONFromMarkdown`） |
| `transcript-utils.ts` | 转录文本合并（`mergeTranscriptSegments`）、文本清洗、分块处理 |

### `/src/lib/services` - 服务层

**v2.1 更新**：新增配额管理和限流服务，统一服务文件风格。

| 分类 | 服务 |
|------|------|
| **数据分析** | analytics-service（会话追踪）, quota-service（API 配额管理）, rate-limit-service（请求限流） |
| **AI/LLM** | llm-service（使用 LLMConfig）, dify-service, tutor-service, web-search-service |
| **ASR** | qwen-asr-service, dashscope-asr-service |
| **核心业务** | meetmind-service, anchor-service, memory-service, classroom-data-service, conversation-service |
| **内容生成** | highlight-service, summary-service, note-service, transcript-enhancer |
| **角色专用** | parent-service, auth-service, wechat-auth-service, teaching-suggestion |
| **工具服务** | email-service, sms-service, verification-code-service, search-service, health-check |

### `/src/types` - 类型定义

**v1.6 类型系统重构**：区分 DB 层和应用层类型，避免 `id` 类型混淆。

| 类型 | 说明 |
|------|------|
| `DBAnchor` | 数据库层困惑点（id: number，对应 IndexedDB 自增主键） |
| `DBTranscriptSegment` | 数据库层转录片段（id: number） |
| `Anchor` | 应用层困惑点（id: string） |
| `TranscriptSegment` | 应用层转录片段（id: string） |
| `BaseTranscriptSegment<T>` | 泛型基础接口，兼容 DB/应用层 |
| `dbToTranscriptSegment()` | DB → 应用层类型转换函数 |

| 文件 | 说明 |
|------|------|
| `index.ts` | 核心业务类型（Anchor, TranscriptSegment, ClassTimeline, DBAnchor, DBTranscriptSegment 等） |
| `user.ts` | 用户/认证类型（User, UserRole, ParentStudentLink 等） |
| `conversation.ts` | 对话类型（ConversationMessage, ConversationHistory 等） |
| `dify.ts` | Dify 服务类型 |
| `onboarding.ts` | 引导系统类型（OnboardingStep, OnboardingState 等） |

---

## 数据存储

### SQLite (Prisma ORM)

服务端数据存储，用于用户信息、认证、数据分析和 API 配额管理。

| 表 | 说明 |
|----|------|
| `User` | 用户基本信息（用户名、邮箱、手机、角色） |
| `AuthProvider` | 第三方登录关联（微信、Google等） |
| `Session` | 用户认证会话 |
| `RefreshToken` | 刷新令牌 |
| `LoginAttempt` | 登录限流记录 |
| `VerificationCode` | 验证码（邮箱/短信） |
| `Feedback` | 用户反馈 |
| `ApiUsage` | API 使用配额记录（v2.1 新增） |
| `UserAnalytics` | 用户分析会话（IP、UA、入口/退出页、时长） |
| `PageView` | 页面访问记录（路径、停留时长） |
| `EventTrack` | 事件追踪记录（录音、困惑点、AI对话等） |

### IndexedDB (Dexie.js)

客户端本地存储，用于课堂数据和缓存。数据库版本 v7，共 12 个表。

| 表 | 说明 |
|----|------|
| `audioSessions` | 音频会话（sessionId, audioBlob, duration, subject, videoUrl 等） |
| `transcripts` | 转录片段（id, sessionId, text, startMs, endMs） |
| `anchors` | 困惑点（id, sessionId, timestamp, type, status） |
| `transcriptLexicon` | 转录词典（term, canonical, aliases, scope, status）v2.7.3 新增 |
| `transcriptEditDiffs` | 转录编辑差异追踪（originalText, correctedText, scope）v2.7.3 新增 |
| `highlightTopics` | 精选片段（id, sessionId, title, segments） |
| `classSummaries` | 课堂摘要（id, sessionId, overview, takeaways） |
| `notes` | 笔记（id, sessionId, content, timestamp） |
| `tutorResponseCache` | AI 家教响应缓存（anchorId, response, model） |
| `conversationHistory` | 对话历史记录（conversationId, type, title, messageCount） |
| `conversationMessages` | 对话消息（messageId, conversationId, role, content） |
| `preferences` | 用户偏好设置（key, value） |

### localStorage

| Key | 说明 |
|-----|------|
| `meetmind_access_token` | JWT 认证令牌 |
| `meetmind_refresh_token` | 刷新令牌 |
| `meetmind_onboarding_state` | 新用户引导状态（已完成/跳过的引导流程） |
| `analytics_visited` | 分析系统新用户标记（v2.0 新增） |
| `app_last_state` | 应用状态（viewMode, sessionId 等） |

### sessionStorage

| Key | 说明 |
|-----|------|
| `analytics_session_token` | 当前分析会话标识（v2.0 新增） |

---

*文档版本：v2.7.3*
*更新时间：2026-02-11*
*更新内容：全量扫描代码更新数量统计（组件100+、Hooks 21个、服务33个、IndexedDB 12表）；新增 useVoiceInput、useNetworkStatus、bilibili-import-service、media-tooling；更新 v2.7.3 录音体验优化*

